name: Build and Release Wails3 App

on:
  push:
    tags:
      - 'v*' # 当推送 v1.0.0 等标签时触发
  workflow_dispatch: # 允许手动触发测试

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  APP_NAME: mmclaw  # 💡 统一配置应用名称

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            platform: darwin
            arch: amd64
          - os: macos-latest
            platform: darwin
            arch: arm64
          - os: windows-latest
            platform: windows
            arch: amd64
          # 💡 新增：Windows ARM64 支持
          - os: windows-latest
            platform: windows
            arch: arm64
          - os: ubuntu-latest
            platform: linux
            arch: amd64
          # 💡 新增：Linux ARM64 支持
          - os: ubuntu-latest
            platform: linux
            arch: arm64

    runs-on: ${{ matrix.os }}
    permissions:
      contents: write  # 允许创建 Release 并上传文件

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1. 安装系统依赖 (Linux 必须)
      - name: Install Linux Dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          # 💡 针对 Linux ARM64 交叉编译，补充 gcc-aarch64-linux-gnu 编译器
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev pkg-config build-essential \
            gcc-aarch64-linux-gnu g++-aarch64-linux-gnu

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      # 2. 安装 Task 和 Wails3 CLI
      - name: Install Tools
        run: |
          go install github.com/go-task/task/v3/cmd/task@latest
          go install github.com/wailsapp/wails/v3/cmd/wails3@v3.0.0-alpha.91
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

      # 3. 针对 Windows amd64 准备 CGO 编译器 (使用稳定的 MSYS2)
      # 💡 针对 Windows arm64，由于 GCC 环境不同，这里加上 arch 过滤
      - name: Install MinGW (Windows AMD64 only)
        if: matrix.os == 'windows-latest' && matrix.arch == 'amd64'
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: mingw-w64-ucrt-x86_64-gcc

      # 4. 构建步骤
      - name: Build with Task
        run: |
          if [ "${{ matrix.platform }}" = "darwin" ]; then
            task package ARCH=${{ matrix.arch }}
          elif [ "${{ matrix.platform }}" = "linux" ]; then
            task package ARCH=${{ matrix.arch }}
          else
            task build ARCH=${{ matrix.arch }}
          fi
        shell: bash
        env:
          # 💡 动态控制 CGO：Windows arm64 交叉编译时默认关闭 CGO 避开本地编译器冲突
          CGO_ENABLED: ${{ (matrix.platform == 'windows' && matrix.arch == 'arm64') && '0' || '1' }}
          GOOS: ${{ matrix.platform }}
          GOARCH: ${{ matrix.arch }}
          # 💡 Linux arm64 使用专属交叉编译器
          CC: ${{ matrix.platform == 'linux' && matrix.arch == 'arm64' && 'aarch64-linux-gnu-gcc' || (matrix.os == 'windows-latest' && matrix.arch == 'amd64' && 'gcc' || '') }}

      # 5. 准备发布产物 (已适配 ARM64 后缀)
      - name: Package Artifacts
        run: |
          # 💡 注意：由于我们在全平台矩阵运行，首先要确保从项目根目录开始算路径
          if [ "${{ matrix.platform }}" = "darwin" ]; then
            cd bin
            zip -r "../${{ env.APP_NAME }}-mac-${{ matrix.arch }}.zip" "${{ env.APP_NAME }}.app"
            
          elif [ "${{ matrix.platform }}" = "windows" ]; then
            # 1. 探测 Wails3 实际的输出目录
            TARGET_DIR="."
            if [ -d "build/bin" ]; then
              TARGET_DIR="build/bin"
            elif [ -d "bin" ]; then
              TARGET_DIR="bin"
            fi
            echo "Found Wails build output directory: $TARGET_DIR"
            
            # 2. 创建一个绝对干净的临时目录
            mkdir -p ../windows-release
            
            # 3. 拷贝产物
            cp "$TARGET_DIR/${{ env.APP_NAME }}.exe" ../windows-release/
            cp $TARGET_DIR/*.dll ../windows-release/ 2>/dev/null || true
            
            # 4. 进入临时目录压缩，带上精准的架构后缀（amd64 或 arm64）
            cd ../windows-release
            7z a "../${{ env.APP_NAME }}-windows-${{ matrix.arch }}.zip" ./*
            
          elif [ "${{ matrix.platform }}" = "linux" ]; then
            cd bin
            # 1. 处理安装包 (AppImage, deb, rpm)
            for f in *.{AppImage,deb,rpm}; do
              if [ -f "$f" ]; then
                ext="${f##*.}"
                mv "$f" "../${{ env.APP_NAME }}-linux-${{ matrix.arch }}.${ext}"
              fi
            done
            
            # 2. 提供 Linux “绿色版”二进制压缩包
            if [ -f "${{ env.APP_NAME }}" ]; then
              mkdir -p linux-release
              cp ${{ env.APP_NAME }} linux-release/
              cp *.so linux-release/ 2>/dev/null || true
              tar -czf "../${{ env.APP_NAME }}-linux-${{ matrix.arch }}.tar.gz" -C linux-release .
            fi
          fi
        shell: bash

      # 6. 发布产物 (无需修改，自动动态识别所有包)
      - name: Upload Release Assets
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            ${{ env.APP_NAME }}-mac-*
            ${{ env.APP_NAME }}-windows-*
            ${{ env.APP_NAME }}-linux-*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
